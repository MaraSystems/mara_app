import { ElementRef } from '@angular/core';
import { UnitDirective } from './unit.directive';

describe('UnitDirective', () => {
  it('should create an instance', () => {
    // const directive = new UnitDirective(el);
    // expect(directive).toBeTruthy();
  });
});
